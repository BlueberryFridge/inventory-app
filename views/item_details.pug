extends layout

block content

    h1= title

    p
        a(href=item_details.category.url)= item_details.category.name
    hr
    p= `Price: Php ${item_details.price}`
    p= `Number in Stock: ${item_details.number_in_stock}`
    p= item_details.description