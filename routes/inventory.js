const express = require('express');
const router = express.Router();

// create new category
router.get('/catgory/create');

// get list of items for one category
router.get('/category/:id', );

// get list of categories
router.get('/category', );

// create new item
router.get('/create', );

// get item details
router.get('/:d', );

// get list of items
router.get('/', );

module.exports = router;